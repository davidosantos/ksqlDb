INNER vs. LEFT vs. FULL Joins
INNER
When a join triggers, an output is emitted if there is matching data for the given key on both sides of the join.

LEFT
When a join triggers, an output is emitted if there is matching data for the given key on the left side of the join. Null values are provided for any missing fields.

FULL
When a join triggers, an output is emitted if there is matching data for the given key on any side of the join. Null values are provided for any missing fields.